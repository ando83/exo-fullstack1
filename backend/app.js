const express = require('express');// importer express

const app = express();//appeler méthode express, créer application express

module.exports = app;//exporter const depuis les autres fichiers serveur node

//on a notre application express en haut (on va retourner server.js)